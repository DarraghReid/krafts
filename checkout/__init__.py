# Tell Django default name for the config class for the app
# Informs Django of custom ready() method for signals to work.
# See apps.py for more on ready() method and imported signals
default_app_config = 'checkout.apps.CheckoutConfig'
